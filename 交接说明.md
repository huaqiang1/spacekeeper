# ChubManager 交接说明

> 最后更新：2026-08-17

## 一、这个项目是做什么的、给谁用

**ChubManager** 是一个本地多账号 Chub AI 角色聊天管理器：

- 把 22 个 Chub 账号（huaqiang1~20 + 主账号等）里的角色和会话**统一拉取**到本地 SQLite 数据库；
- 在 Web 面板里查看"**聊过的角色**"（按角色合并、跨账号标注）、展开会话**继续对话**（回复由本地生成，**不回写** Chub 官方会话）；
- 管理账号（增删、密码查看、一键恢复配额）。

**给谁用**：给需要同时维护多个 Chub 账号角色的人使用；本次是项目交接，供接手开发者 / 智能体快速上手。

## 二、技术栈

| 层 | 技术 |
|---|---|
| 后端 | Node.js + Express + better-sqlite3 + Puppeteer（无头 Chrome 登录 Chub、调用其内部 API） |
| 前端 | React + Vite + Ant Design |
| 数据 | SQLite（`backend/chub_data.db`） |
| 部署 | 后端直接托管前端构建产物（`frontend/dist`），单端口 **3333** |

- Chrome 固定路径：`C:\Program Files\Google\Chrome\Application\chrome.exe`（Puppeteer 直接复用系统 Chrome）
- 登录态：登录后 token 存在 `chub.ai` 域的 `localStorage.URQL_TOKEN`，所有 API 调用都在页面内 `fetch` 并带 `samwise` / `ch-api-key` 头（`chub-api.js: apiCallForAccount`）

## 三、怎么安装、怎么跑起来

```bat
:: 1. 安装后端依赖（backend 目录）
cd backend
npm install

:: 2. 构建前端（frontend 目录）
cd ..\frontend
npm run build

:: 3. 启动（根目录双击 start.bat 或手动）
cd ..\backend
node server.js

:: 4. 访问
http://localhost:3333        （本机）
http://<局域网IP>:3333       （其他机器/智能体）
```

- 数据库账号在 `backend/chub_data.db` 的 `accounts` 表（存邮箱 / 密码 / 用户 ID）；
- 会话数据通过 `/api/import/...` 或启动流程从 Chub 拉取；
- **后端重启流程**：读 `backend/server.pid` → `Stop-Process -Id <pid>` → 删除 pid 文件 → `Start-Process node server.js`（Hidden 窗口）→ 等待 8~10 秒；
- `POST /api/accounts/quotas` 会重新登录全部 22 个账号并刷新配额（当前全部 trial=1000 / sub=0）。

## 四、已完成的功能

1. **22 账号接入**：无头 Chrome 自动登录 Chub，token 持久化，API 调用全部可用；
2. **角色 / 会话统一拉取**：17 个唯一角色（按 chub_id 合并）、22 条会话；只导入"聊过的角色"；
3. **聊过的角色板块**：按 chub_id 聚合、按 `chub_updated_at` 倒序、跨账号标注所属账号、点击展开会话继续对话；
4. **账号管理**：添加 / 删除账号、密码掩码与显示切换、"选择已记录的账号"快速填表、一键恢复全部配额；
5. **完整角色卡**：`GET /api/characters/{id}?full=true` 获取完整卡（字段在 `node.definition`：personality / scenario / first_message / example_dialogs / system_prompt / post_history_instructions / embedded_lorebook 等），17/17 角色已补全完整卡；
6. **占位符渲染**：`{{char}}` / `{{user}}` 在卡面字段、世界书、历史消息中的替换（`renderPlaceholders`）；
7. **本地生成回复**：模型直连生成，支持流式，不回写 Chub 官方会话。

## 五、还没做的 / 已知问题 / 卡住的点

### 5.1 【卡住】本地生成的角色贴合度仍不如原网站
已逆向出站点 prompt 组装规则（来源：`www.chub.ai` 前端 bundle 分析）：
- 模板 K2t：`{{jailbreak_prompt}}` + `\nAbout {{char}}: {{personality}}` + `\nFacts about the world/environment: \n{{lore}}` + `\nCircumstances and context of the dialogue: {{scenario}}` + 示例对话段（`\nExample conversations between {{char}} and {{user}}:\n{{example_dialogue}}\n[End of examples. Begin real interaction.]`，无示例则只有 `[Begin real interaction.]`）；
- 默认 jailbreak：`You'll portray {{char}} and engage in Roleplay with {{user}}. You are encouraged to drive the conversation forward actively...`（完整文本见抓取记录）；
- 消息模板 uce：消息内容前加 `{名字}: ` 前缀（assistant=角色名，user=用户名），并带 `name` 字段；
- `post_history_instructions` 作为**独立 system 消息**插在历史之后、当前用户消息之前；
- 历史消息数组为**逆序**（[n-1, n-2, …, 1]），然后 posthistory，最后当前消息；
- 采样默认：temperature 0.8 / top_p 1 / top_k 30 / presence_penalty 0.8 / frequency_penalty 1 / min_p 0.1 / stop `["#","\nUser ","###","<!"]`（具体按模型预设，未完全确认）。

**尚未**在 `generateResponse` 中逐字复刻上述逻辑——这是下一步最重要的工作。

### 5.2 【卡住】站点真实 /prompt 请求抓取未完成
想通过"在网站页面里发一条消息、抓 `/prompt` 请求体"来拿到精确的最终 prompt，但 headless 页面**输入框不渲染**。已查明的原因链：
1. 通过 API 新建的会话默认 `is_public=true`，公开会话页面为**只读预览**（无输入框）→ 已用 `PATCH /api/core/chats/{id} {is_public:false}` 解决；
2. 必须访问 `https://chub.ai/chats/{id}`（**不能带 www**）——`URQL_TOKEN` 存在 `chub.ai` 域 localStorage，www 是另一个源，会导致未登录态；
3. 聊天页 chunk（`index-20260628T001015484Z-Cib7MR7g.js`）中输入框渲染条件：`m && c.id!=0 && r!=null && ye && d!=null && r.config.api==d.api && r.config.model==d.model`；已确认 generator 存在且 api/model 匹配（chub/soji），但 textarea 仍不出现，`ye`（登录用户是否为会话参与者）与 `m`（消息加载完成）尚未最终确认。

调试工具：`POST /api/debug/promptcap`（创建/复用会话 → 页面加载 → 注入文字 → 监听 `ro.chub.ai/prompt` POST → 删除临时会话），并带 React fiber 上下文抓取（输出到 `ctx_state.json`）。调试产物在 `C:\Users\chen-huaqiang\AppData\Local\Temp\opencode\`：
- `chub_app.js`（站点主 bundle，12.9MB，prompt 逆向依据）
- `chat_page.js`（聊天页 chunk）
- `site_prompt.json`（未捕获成功）、`ctx_state.json`（页面 React 状态）
- 若文件被清理，重新下载：`https://www.chub.ai/assets/App-20260628T001015484Z-BMBJ9rj-.js`、`https://www.chub.ai/assets/index-20260628T001015484Z-Cib7MR7g.js`

### 5.3 【遗留】临时调试端点
`server.js` 中 `/api/debug/raw`、`/api/debug/promptcap` 为调试用，**正式分发前应删除**。

### 5.4 已知小坑
- debug 端点经 `result.text` 的响应在 **50002 字节截断**，需走 `result.json`（rawurljson 模式）；
- Windows PowerShell 5.1 控制台为 GBK 编码，中文输出乱码——调试时把内容写文件再用 UTF-8 读取；
- 本地测试脚本若 `require('better-sqlite3')`，脚本必须放在 `backend` 目录内运行（node 模块按脚本目录解析）。

### 5.5 安全提醒
数据库保存 22 个账号的**明文密码**，且 `/api/accounts` 接口会返回 `password` 字段。**不要**把本仓库、压缩包或数据库公开或发给未授权对象。

## 六、关键文件

| 文件 | 作用 |
|---|---|
| `backend/server.js` | Express 服务、全部 `/api` 接口、静态托管 `frontend/dist`、调试端点 |
| `backend/chub-api.js` | Puppeteer 登录、sessions、`apiCallForAccount`、`generateResponse/Stream`、`buildSystemPrompt`、`renderPlaceholders` |
| `backend/chub_data.db` | SQLite：`accounts` / `sessions` / `characters` 表 |
| `frontend/src/App.jsx` | React 界面（`RecentsView` 聊过的角色、`AccountsView` 账号管理） |
| `frontend/src/App.css` | 界面样式 |
| `start.bat` | 一键启动（生产模式仅后端，托管 dist） |

## 七、打包产物

`ChubManager-package.zip`：内含后端全部代码 + node_modules + 数据库 + 前端 dist + `start.bat`，解压后双击 `start.bat` 即可运行（仅限 Windows x64 + 本机已装 Chrome 的环境）。